const AuthService = require('./Auth');
const FS = require('./FS');
const Prompt = require('./Prompt');
const XMLManager = require('./XMLManager');

module.exports = {
    AuthService,
    FS,
    Prompt,
    XMLManager
}
