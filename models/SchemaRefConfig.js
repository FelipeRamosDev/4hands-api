const SchemaRefConfig = require('../src/models/settings/SchemaRefConfig');

/**
 * @type {SchemaRefConfig} - SchemaRefConfig model to config the fields relations between collections.
 */
module.exports = SchemaRefConfig;
